apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: {{ .Name }}
  namespace: default
spec:
  template:
    spec:
      {{ if ne .ImagePullSecrets "" }}
      imagePullSecrets:
        - name: {{ .ImagePullSecrets }}
      {{ end }}
      containers:
        - image: {{ .RemoteTag }}
          imagePullPolicy: Always
